---
ai_role: system_constraint
scope: shadowsocksbrowser_swift_code_generation
priority: critical
enforce: always
version: 1.0
---

# ShadowsocksBrowser Swift 约束

## 适用范围

本文件定义仓库内的 Swift 工程约束，适用于：

- `App/`
- `PacketTunnel/`
- `Packages/SharedCore/`
- `Tests/`

> 目标：目标是让代码保持轻量、可测试、原生、可持续演进，避免出现巨型文件、职责混杂和难以维护的边界。让 AI 和人工审查都能快速执行。
> 原则：规则少而硬，优先约束结构、复杂度、命名和工作流。




## 1. AI 执行方式

- 默认采用“自主执行”模式，不为轻微不确定性反复提问。
- 先自行读取代码、配置、README、现有实现，再按仓库惯例决策。
- 可合理推断的信息直接假设并继续，优先最小改动、最低风险、最易回滚方案。
- 非关键分歧不输出选项题，不先问“要不要继续”，直接做到可交付结果。
- 仅以下场景允许中断提问：删除或覆盖数据、缺少必要凭证或外部 ID、会改变架构边界或公共 API、需求歧义已影响安全落地。
- 任务完成后直接输出结果、关键假设、验证结论；不输出冗长过程性追问。
- 若验证失败，先继续修复；已无法安全推进时再一次性说明阻塞点。
- 遇到上下文压缩窗口，或判断当前轮结束后即将触发上下文压缩时，必须先写一份 handoff 文档，至少记录当前目标、已完成项、待处理项、关键文件、关键假设、未验证风险与下一步动作；写完后再结束当前轮，避免因上下文压缩造成有损开发。
- 用户发出提交指令（如“提交”、“commit”、“推送”等）即视为完整授权：按第 7 节流程对工作区内容分类提交并推送远端，无需二次确认；仅在“不确定某改动是否适合提交”或“推送存在冲突”时中断询问。

## 1.1 最小侵入原则

- 默认最小改动，禁止大开大合。
- 能局部改，不整段重写；能新增薄层，不整体重构；能复用现有结构，不新造抽象。
- 未经明确要求，不得扩大文件范围、职责边界或调用链长度。
- 功能、修复、重构分开处理，不把“顺手优化”混入当前任务。
- 输出方案时先给最小落地路径，再谈备选大方案。
- 若必须扩大改动，必须先说明局部方案为何不可行。

## 1.2 长期设计原则

- 设计方案从长期出发，优先选择能长期维护的结构，拒绝只为短期生效的权宜实现。
- 评估方案时把"三个月后的人能否读懂、改得动"作为硬指标：命名、分层、依赖方向必须对人类可读。
- 短期 hack（补丁式判断、特殊分支、一次性脚本进主干）默认禁止；确属例外必须按第 8 节加 `// REASON:` 并说明何时清理。
- 最小侵入不等于短视：局部改动也要落在正确的职责边界内，不把债务留给后续维护者。
- 输出方案时说明长期取舍：该结构如何支撑后续扩展、阅读与回滚。



## 强约束

- 统一使用 Swift 5.9+ 现代语法。
- 必须使用纯 Swift，不得引入 Objective-C 桥接、混编 target、CocoaPods 或外部 helper 进程。
- 依赖管理统一使用 Swift Package Manager，优先使用系统框架和本地 package。
- `project.yml` 是工程结构唯一真实来源，不以手改工程文件作为主要工作流。
- 优先低侵入改动，先沿用现有边界，再决定是否抽新层。
- 必须保护极速启动和稳定的视觉连续性，避免引入闪烁、阻塞启动、无意义中间态。

## 模块边界

### `App/`

- 负责 SwiftUI 视图、UI 状态编排、浏览器交互和 App 侧协调逻辑。
- 不得承载包解析、加密细节、路由核心规则或协议编解码实现。
- View 文件应聚焦展示和 UI 事件绑定。

### `PacketTunnel/`

- 负责隧道生命周期、包流转、TCP/UDP relay 和扩展侧运行时行为。
- 不得依赖 SwiftUI 或 App 专属展示逻辑。
- 副作用必须清晰，网络 IO、包改写、隧道状态迁移都要易于追踪。

### `Packages/SharedCore/`

- 负责可复用纯逻辑，包括 model、parser、store、路由规则、协议编码和可测试辅助能力。
- 可复用业务逻辑优先下沉到这里，避免在 `App/` 或 `PacketTunnel/` 重复实现。
- 该模块代码应尽量脱离 App 与扩展运行时即可完成单元测试。

### `Tests/`

- 测试必须遵循模块边界。
- `SharedCore` 行为优先落在 package tests。
- App 与隧道的集成行为放在 `Tests/ConnectionIntegrationTests`。

## 代码结构红线

- 每个函数或方法体不超过 20 行，不含注释、空行和纯括号行。
- 每个函数参数不超过 4 个，超过时必须引入 `struct` 或配置对象。
- `View.body` 不超过 15 行，超过必须拆分子 View 或提取 `@ViewBuilder`。
- 每个主要 `class`、`struct`、`enum`、`actor` 原则上不超过 100 行。
- 每个文件只定义一个主要类型，相关 `extension` 可同文件或按职责独立拆出。
- 文件必须小而专一，理想情况下单文件控制在 100 行以内。
- 当一个文件同时开始承担布局、状态变更、持久化、网络或解析职责时，必须拆分。
- 不允许因为“还能编译”就继续往一个类型里堆叠无关职责。
- 扩展拆分要服务于可读性，不要在没有明确边界的情况下把一个类型零散拆到很多文件。
- 单个函数内分支总数不超过 3，嵌套深度不超过 2 层。
- 单个条件表达式中的 `&&` 或 `||` 不超过 2 个。
- 禁止嵌套三元运算符，优先使用 `guard` 和 early return 拉平控制流。

## 类型设计

- 值类型模型和配置数据优先使用 `struct`。
- 共享可变状态、delegate、manager、长生命周期协调器优先使用 `final class`。
- 优先使用组合而不是继承，通过 `protocol` + `extension`、依赖注入、`ViewModifier` 复用能力。
- 模块边界处优先使用窄协议，尤其是测试替身和依赖替换确实有价值时。
- 没有第二实现、没有测试价值、没有明确所有权边界时，不要为了抽象而抽协议。
- 数据结构必须显式，能用 Swift 类型表达的内容不要退化成字典式传递。

## 命名

- 类型名应表达领域语义，而不是界面位置或临时实现细节。
- 方法名应像动作，属性名应像数据。
- 布尔命名禁止使用 `is`、`has`、`can`、`should` 前缀，优先使用 `connected`、`loading`、`selected`、`enabled`、`empty`、`refreshNeeded` 这类形式。
- 同一概念在不同模块内应保持统一术语，除非存在明确的翻译边界。
- 字符串型资源 key 等标识符统一使用 `snake_case`。
- 使用标准驼峰命名，避免无意义缩写；`URL`、`JSON`、`HTTP` 等通用缩写除外。

## Swift 现代语法

- 优先使用 `async/await`，禁止新增多层 `CompletionHandler` 回调链。
- 同步或异步失败使用 `throws` 或 `Result` 显式表达，不得混成难以追踪的隐式失败路径。
- 禁止使用隐式解包可选 `T!`，仅保留 Apple 框架强制要求的场景。
- 优先使用 `guard let` 或 `if let` 解包，避免强制解包 `!`。
- `??` 只用于提供默认值，不用于掩盖错误来源。
- JSON 解析优先使用 `Codable`，不要手写字典式解析主流程。
- 集合操作在可读性不受损时优先使用 `map`、`filter`、`reduce`、`forEach` 等高阶函数。

## SwiftUI 约束

- `View.body` 必须保持声明式，不要在内联闭包或条件分支里藏复杂业务逻辑。
- 不要在 SwiftUI 模板中直接调用重逻辑函数，应在 ViewModel 或轻量派生属性中预先整理状态。
- SwiftUI View 只拥有必要的本地状态，可变状态必须尽量只有一个明确 owner。
- `@State` 只用于局部、短生命周期的 UI 状态。
- `@StateObject` 只用于当前视图拥有生命周期的引用类型对象。
- 当所有权在外部时，使用 `@ObservedObject` 或注入式依赖。
- 状态传递优先级为 `@State` -> `@Binding` -> `@ObservedObject` -> `@EnvironmentObject`，不要滥用 `@EnvironmentObject`。
- 导航、sheet、overlay 的触发条件必须清晰可追踪。
- 子 View 与父 View 通信优先使用 `Binding` 或闭包回调，不直接改写外部状态。
- 提取复用 UI 片段是为了降低理解成本，不是为了机械压缩文件长度。
- 优先使用 `NavigationStack` 或 `NavigationSplitView`，不要新增 `NavigationView`。
- 禁止新增已废弃的 SwiftUI 或 UIKit API。
- 布局优先使用 `VStack`、`HStack`、`ZStack`、`Spacer()` 和弹性 frame，谨慎使用 `GeometryReader`。
- 文本默认支持 Dynamic Type，除非品牌要求明确排除。

## ViewModel 与状态规则

- ViewModel 负责把领域状态转换成 UI 可消费状态，不得演变成杂物仓库。
- 异步任务、持久化调用和副作用编排不得塞进叶子视图。
- 优先提供显式输入方法，如 `load()`、`refresh()`、`connect()`、`disconnect()`、`save()`，避免从多个调用点做隐式状态写入。
- 对外暴露稳定状态面，不要把大量可变内部细节直接开放给视图。
- 一个类型一旦同时管理多个相互独立的关注点，应先拆分再继续加状态。

## 并发

- 优先使用 Swift Concurrency，包括 `async/await`、`Task`，必要时再使用 actor。
- UI 相关状态更新必须在正确的主线程语义下完成。
- 重要任务不得随意发起无归属、不可取消的 fire-and-forget `Task`。
- 需要取消语义的流程必须显式传播取消，尤其是隧道控制、刷新链路和网络驱动 UI。
- 并发边界必须明显，禁止隐式后台写状态。
- 闭包捕获引用类型时要明确处理生命周期，必要时使用 `[weak self]`。

## 错误处理

- 调用方可恢复或可分流处理的失败，必须使用显式类型错误表达。
- 不得静默吞错。
- 禁止空的 `catch {}`。
- 低层错误转换为用户可见文案，只能发生在展示边界。
- 优先早返回和显式失败路径，避免深层嵌套控制流。
- 日志应帮助定位运行问题，但不能把主路径写成调试叙事。

## 依赖

- 优先使用 Apple 系统框架和现有本地模块。
- 新增依赖必须有明确收益，且原生方案确实不足。
- 新依赖必须符合纯 Swift 与 SPM 集成约束。
- 不得引入与现有仓库能力重复的 UI、网络、持久化框架。

## 共享逻辑归属

- 解析、存储编解码、路由规则、协议层转换优先归属 `SharedCore`，除非运行时 API 限制必须放到别处。
- 隧道专属的包流转和扩展运行时协调留在 `PacketTunnel`。
- 浏览器展示、sheet、菜单和 SwiftUI 组合逻辑留在 `App`。
- 在 `App/` 增加工具能力前，先判断是否应下沉到 `SharedCore`。

## 性能与内存

- 避免在 `body` 中创建大量临时对象或触发重计算。
- `ForEach` 必须提供稳定 `id`，或让数据类型实现 `Identifiable`。
- 大数据列表优先使用 `LazyVStack` 或 `LazyHStack`。
- 不要在 `onAppear` 或 `onChange` 中直接执行重任务，改用有归属的异步任务。

## 测试规则

- 新增解析、路由、存储和协议逻辑时，必须补对应单元测试。
- 修 bug 时，原则上要补上能拦住该回归的测试。
- 单元测试无法证明的边界，使用集成测试覆盖，尤其是 tunnel flow、relay 行为和生命周期协调。
- 测试必须可读，helper 用来去掉样板噪音，不用来隐藏被验证的行为。

## 注释与文档

- 公共 API 在有外部调用价值时应补 `///` 文档注释。
- 复杂逻辑只解释“为什么”，不要逐行复述“做了什么”。
- 使用 `// MARK:` 组织较长文件中的职责分区。
- 不要保留大段注释掉的旧代码，历史交给 Git。

## 变更纪律

- 以保持清晰为前提做最小改动。
- 能拆开的情况下，重构与行为改动分开提交。
- 在聚焦任务中，不顺手做大面积重命名、广泛移动文件或无关风格清洗。
- 除非任务明确要求，不改变现有用户可见行为。

## 零容忍项

- 函数体超过 20 行且未拆分。
- `View.body` 超过 50 行且未拆分。
- 主要类型超过 100 行且职责仍然混杂。
- 函数参数超过 4 个且未封装。
- 分支总数超过 3 或嵌套深度超过 2 且未拉平。
- 布尔命名使用 `is`、`has`、`can`、`should` 前缀。
- 新增已废弃 API。
- 强制解包 `!` 或隐式解包 `T!` 出现在非必要场景。
- 空 `catch`、吞错、无说明忽略失败。
- 新增回调地狱而不是使用 Swift Concurrency。

## 交付前检查

提交前确认：

- 代码是否位于正确模块。
- 文件是否仍然只有一个清晰职责。
- 视图层是否越界承担了领域逻辑或隧道逻辑。
- 可变状态是否只有一个明显 owner。
- 异步任务是否有明确生命周期。
- 新逻辑是否有最窄但足够的测试覆盖。
- 是否引入了没有明确必要性的依赖或抽象。
- 是否触碰了任何零容忍项。



## 工作流要求

- 每次完成一次明确的代码改动后，默认执行一次 Git 提交。
- 执行提交时必须调用 `git-commit` skill，按其流程操作，禁止绕过 skill 直接手写 git 命令。
- 用户发出提交指令时，需对当前工作区全部内容按逻辑分类提交：单一改动一次提交，多组无关改动按逻辑分组多次调用 `git-commit` 拆分提交。
- 提交前先检查改动范围，避免把无关文件混入同一个 commit；提交信息必须准确描述本次改动，保持历史干净。
- 提交完成后执行 `git push` 推送到当前远端分支；提交指令本身即包含推送授权，无需再问。
- 仅以下场景允许中断询问：不确定某改动是否适合提交（如疑似无关文件、敏感信息）、推送或合并存在冲突。
- 若推送失败、存在冲突、鉴权失败，立即报告原因，不伪造“已推送成功”。
